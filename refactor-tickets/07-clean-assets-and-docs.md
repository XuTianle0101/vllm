# 07: 清理历史实验资产并收敛文档

**What to build:** 使用者可以从精简文档完成启动、理解核心约束、复现当前验证并了解已知问题，代码树只保留当前验证和标准路径性能测量必需的资产。

**Blocked by:** 03 — 将保留验证迁移到标准执行路径；06 — 集中 KSA 语义并简化上游接入。

**Status:** ready-for-agent

- [ ] 删除旧 baseline、T01–T06 阶段比较、普通 Qwen3 性能 baseline，以及仅服务旧实验完整重放的脚本、冻结资产和依赖；先确认保留验证不再引用它们。
- [ ] 保留最小 HF reference、最小复现输入、原容差、必要小型 fixtures、layout/visibility 语义验证和当前标准路径性能测量能力；普通 Qwen3 功能回归继续可运行。
- [ ] 历史报告、大体积机器结果、重复输入和阶段性过程材料退出代码树，不要求完整重放旧实验；将仍有价值的已知问题说明保留在精简文档中。
- [ ] 文档收敛为启动说明、核心设计约束、验证方法、已知问题四类，去除失效引用，并给出实施时实际核实的命令和环境要求。
- [ ] 启动说明解释标准 `vllm serve`、checkpoint 识别、`ksa_cudagraph` 控制内部局部 decode graph 及其与 `enforce_eager=True` 共存的含义；保留外层启动便利封装。
- [ ] 已知问题保留历史 NEEDS_FIX、54/69 通过与 15/69 失败、长度 1023/1024/1031/1032/1033、最大 logits 误差 35.75 和 logprobs 误差 14.35148，并明确历史结果不代表当前版本验证通过。
- [ ] 验证精简后的依赖仍支持保留工作流，结果区分既有失败、新增回退与未执行项；不放宽容差，不删除失败复现，不夸大 dtype 支持范围。
