# vLLM KSA 整理方案

## 状态与范围

本方案记录 grilling 已收敛的设计取舍。2026-09-18 已按用户指令完成票 01 的整理前验证与性能基线，见 [基线报告](docs/ksa/refactor-baseline.md)。票 02 已完成最小 HF reference 与冻结复现输入提取，见 [验证说明](docs/ksa/hf-reference.md)。票 03 已完成保留验证的标准路径迁移，见 [标准验证](docs/ksa/standard-validation.md)。生产代码整理与 Git 历史重写尚未开始；后续按任务票推进。

- 目标仓库：当前目录（`vllm` 仓库根目录）。
- 当前分支：`releases/v0.26.0-ksa`。
- 调查时 HEAD：`81d2c510ef`。
- 整理范围：最近 28 个 commits，覆盖 T00–T07 的 KSA 支持。
- 基点：`568afb3a13`，对应此次 KSA 改动之前的 vLLM v0.26.0 基点。
- 调查时工作区干净，远端同名分支与本地 HEAD 一致。实施前重新核对，避免覆盖后续改动。

目标是为后续 KSA 开发建立清晰、精简、可验证的代码基础。先整理代码并验证，再按职责将这 28 个 commits 收敛为少量 commits。

## 设计原则

优先级：**KSA 的 locality > 减少对上游 vLLM 的侵入 > 减少代码量**。

- 围绕当前 KSA 行为整理，不提前扩展其他模型、summary 策略或分布式能力。
- 将 summary 语义、缓存约束等复杂行为集中在明确的 module 内，通过小 interface 接入 vLLM。
- interface 包括参数、配置、不变量、调用顺序、错误行为与性能约束；简化不能只看函数数量或代码行数。
- 只为真实变化设置 seam，不为假设中的 adapter 增加间接层。
- 验证通过有意义的 interface 观察行为，移除仅服务于已删除实现的测试。

## 标准执行路径

- 使用标准 `vllm serve` 启动，根据 checkpoint 配置识别 KSA。
- 删除 `vllm/entrypoints/ksa.py` 专用 HTTP 入口、`KSABatchedRunner` 独立 runner，以及其他旧的专用生成入口。
- 调度、采样和页生命周期统一由 vLLM 标准路径管理，保留标准采样、seed、多样本、惩罚、stop、logprobs、SSE 等现有行为。
- 保留生产路径使用的 `KSAGPUModelRunner` 及必要的 scheduler 页映射；删除独立 `KSAPagePool` 分配器、模型 `new_cache()` 等仅用于旧路径的 interface。先迁移其验证调用，再删除依赖。
- 必要的 reference implementation 留在验证侧，不继续作为生产执行选项暴露。
- 检查并合并重复的模型识别、summary 预算计算和配置语义，避免建立仅转发调用的浅 module。

### Graph 与兼容范围

- 本次保留 `--additional-config '{"ksa_cudagraph": true}'` 配置键。
- 该配置控制 KSA 内部局部 decode graph，并与 `enforce_eager=True` 共存；启动文档必须解释这层含义。
- 本次不将其迁移到 vLLM 标准 compilation/cudagraph 配置，该集成改造另行开展。
- 保持现有功能范围和限制，不将清理变成支持矩阵扩张。BF16 是已有验证范围，不将其他 dtype 描述为已验证，也不因整理擅自新增 dtype 禁用限制。
- 保持普通 vLLM 使用行为兼容。
- 外层 `../server_ksa.sh` 已是标准 `vllm serve` 的便利封装，且不属于 vLLM 仓库，本次保留。

## Baseline、实验与文档清理

仅保留下列验证能力：

1. HF 正确性对照。
2. 最小可复现输入和原有容差。
3. 当前标准执行路径的性能测量。

删除 T01–T06 阶段比较、普通 Qwen3 性能 baseline，以及仅服务旧实验完整重放的脚本、冻结资产和依赖。保留普通 Qwen3 功能回归。

现有最终验证脚本直接依赖 `baseline.py` 的部分函数，不能仅按文件名删除；先抽取所需的最小 reference module，并将执行侧迁移到标准 vLLM 路径，再删除旧实验体系。必要的 layout、visibility 等语义验证可以保留。

文档只保留：

- 启动说明。
- 核心设计约束。
- 验证方法。
- 已知问题。

历史报告、大体积机器结果、重复输入及阶段性过程材料退出代码树。不要求完整重放旧实验；验证必需的小型 fixtures 随验证代码保留。

## 已知问题与本次修复范围

已有 `docs/ksa/results/T07/a100-final/README.md` 报告标记为 **NEEDS_FIX**：HF 完整生成轨迹 54/69 通过、15/69 失败，涉及输入长度 1023、1024、1031、1032、1033；最大 logits 误差 35.75，最大 logprobs 误差 14.35148。

这些是历史报告中的结论；票 01 已重新执行完整 HF 轨迹，仍为 54/69 通过、15/69 失败，失败集合与四项门禁指标无新增恶化。整理时需保留相应失败复现、输入、原容差及问题说明，不将旧报告中的通过结果视为新代码的验证结果。

票 01 另观察到一次 HTTP eager 固定 seed/n=2 响应不一致，同配置独立复测及历史配置 eager/graph 均通过；原因未定，失败记录与补测均保留在基线报告中，后续不得忽略该现象。

本次聚焦整理，不承担上述精度问题的根因诊断与修复；要求不引入新回退。不得通过放宽容差或删除失败用例使验收显示通过。已知问题修复另行开展。

## 验证与验收

实施前明确保留的 module 契约及最低有效测试层级，遵守 `AGENTS.md` 中的环境、测试和评估要求。

- HF 对照覆盖 summary/窗口边界、分块 prefill、多步 decode，并保留已知失败用例及原容差。
- 标准执行路径覆盖 eager/graph、批处理、取消与缓存复用。
- 保留普通 Qwen3 功能回归。
- 性能选短、中、长上下文，进行整理前后同环境比较，不重跑历史完整长度与并发矩阵。
- 在改动前确定代表性用例、环境和测量口径，记录必要的整理前结果，以便改动后比较。
- 根据实际修改运行相关测试、静态检查及模型评估。对环境导致无法执行的项目明确记录，不将其计为通过。
- 验收记录区分既有失败、新增回退与未执行项目；若出现新增回退，先解决再完成整理。

具体命令、模型位置、硬件可用性和性能判定细节在实施时调查确定，不凭历史结果推定。

## 实施顺序

1. 核对工作区、提交范围、适用仓库规则及运行环境，记录必要的整理前验证与性能结果。
2. 提取最小 HF reference 和输入/容差，将保留的验证迁移到标准执行路径。
3. 删除专用入口、独立 runner、独立页分配器与失去用途的 interface，集中 KSA 语义并简化 vLLM 接入修改。
4. 删除旧 baseline、阶段实验资产和冗余文档，补齐四类保留文档。
5. 完成相关检查、功能验证、HF 对照与代表性性能比较，确认无新增回退。
6. 根据最终代码职责组织少量 commits，完成本地历史重写，报告验证结果及剩余已知问题。

## Git 历史与提交身份

- 先完成代码整理和验证，再重写本地当前分支最近 28 个 commits。
- 最终保留少量按职责划分的 commits；具体数量根据实际变更决定。
- 不创建保存旧 28 个 commits 的归档分支、tag 或其他归档引用。不以本次整理为由额外清理 reflog 或强制 Git 垃圾回收。
- 原有 28 个 commits 已在远端；**本次暂不推送远端，也不 force-push**。
- 所有新提交的 author 必须为 `xutianle <xutianle@fudan.edu.cn>`，提交后核验实际 author 字段。
- 遵守仓库要求的提交归属 trailer，避免为本任务修改全局 Git 身份配置。
