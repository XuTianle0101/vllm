# T07：最终精度与性能验收

- 状态：IN_PROGRESS
- 依赖：T06 DONE
- 目标：验证最终交付满足 Transformers 精度与性能目标。

## 实现范围

汇总安装、启动、环境锁定、功能限制和完整实验命令，复核自动模型识别与普通 Qwen3 回归。提供端到端报告、原始指标和可复现实验配置。只修复验收发现的问题，不扩展首版范围。

## 服务器实验

运行总计划的精度矩阵、长距离检索、基础服务、eager/graph、生命周期与抢占测试。性能覆盖 4K/16K/32K/64K 和接近 128K 上限场景，输出长度预留在 131072 文本上限内；并发 1/4/8，保留 OOM 记录。

同卡顺序运行固定的官方 HF 与最终 vLLM，预热后至少 5 次，记录运行时差异及波动。单请求直接比较，并发结果在 HF 不支持时单列。

## 完成门禁

- 满足 T00 冻结精度阈值，所有未解释差异关闭。
- 16K、32K、64K 单请求 decode 均优于官方 HF，收益超过测量波动。
- 真实 KV 压缩成立，128K 单请求推理无 OOM。
- TTFT、短上下文或 prefill 回退全部披露和分析。
- 必需功能、文档、测试和原始数据齐全；未执行的项目不得计为通过。

## 交付记录

- 实现 SHA：`b31a809bce7ebc45c472df008d3de4a69ebe7c6a`；最终验收记录待实验结束；baseline ID：`T00-305bf8f9dc429f1af60e`。
- 本地检查：扩展 GPU/CPU 回归 132 项通过，验收脚本契约测试 9 项通过；8 并发五次均无重捕获。
- 服务器命令：[完整安装、启动与实验命令](../t07-final-validation.md)。
- 结果路径：工作区 `../results/T07/`，实验执行中。
- 最终结论：执行中，未计为通过；[验收报告](../results/T07/a100-final/README.md)。
