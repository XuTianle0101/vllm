# T05：CUDA Graph 与瓶颈定位

- 状态：DONE
- 依赖：T04 DONE
- 目标：完成图执行，基于实测选择下一阶段算子工作。

## 实现范围

预分配 GPU metadata 与执行缓冲区，稳定地址和图重放尺寸。覆盖普通 decode、块末文本加 summary、二者混合批次及请求槽复用，保留 eager 对照。

提供 profiling 入口，分解投影、attention、KV 管理、Python/CPU 同步与图开销。记录启动编译/捕获成本和稳态成本，不将二者混算。

## 服务器实验

比较 eager/graph 精度，连续重放并反复改变请求长度和块相位，检查状态污染。测量不同长度与批量下图优化收益，保存必要 trace 和热点摘要。

## 门禁与反馈

eager/graph 满足精度阈值且稳定运行。输出 T06 的明确路径选择依据：官方算子接口、许可证、5090 支持、语义、分页/图兼容性与预期瓶颈。未测量的收益不得填为事实。

## 交付记录

- 实现：[运行说明](../t05-cudagraph-profiling.md)。固定 GPU 缓冲区、普通/块末/混合
  decode 图重放，有界图缓存和槽复用；标准 V1 可显式开启，保留 eager 默认行为。
- 核心代码与性能 SHA：`d38983dd881a9766f0349254d5b0b6fda95b3753`。
- 最终代码/V1 验证 SHA：`bc74fbe2c2b8099c987ee3d38eeda35027ef8418`。
- 检查：最终 82/82 项回归通过，其中两项真实 CUDA 捕获；
  全部适用 pre-commit 检查通过。
- A100：冻结教师强制 68/68；1408 次 replay；全部页归还。
  标准 V1 再完成 62 次 replay、8/8 同前缀比较，生成分歧单列报告。
- 性能：1K/4K × batch 1/4/8、32 步、一次预热+五次重复，启动与稳态分开。
  1K/batch=1 为 2.043×，4K/batch=8 退化至 0.399×；不宣称全局收益。
- 服务器命令：见运行说明；本轮已在当前 A100 服务器执行。
- 结果：[验收报告](../results/T05/a100-d38983dd88/README.md)，
  原始 trace/logits 留在 `../results/T05/`，摘要保存文件哈希。
- T06：[算子路径决策](../t06-kernel-decision.md)，prefill/decode 均选择仓库内 Triton，
  优先直接分页读取和融合 attention，消除完整历史 staging/GQA 重复。
- 结论：T05 A100 精度与稳定性门禁通过；性能退化已记录。prefill 仍 eager，
  5090 和 T06 算子收益未测，不纳入本次验收。
