# T07 A100 最终验收

状态：NEEDS_FIX。HF 完整生成轨迹发现精度超限，不计为最终通过。
按用户要求不测试 128K×4/8；范围内 23 个性能任务和最终回归已完成。

代码提交：`b7194a983e0fd5f7fa526b03bf6210edb2b8113a`。
分支：`releases/v0.26.0-ksa`。
原始数据：工作区 `results/T07/`（vLLM 仓库上一级）。
[完整复现命令](../../../t07-final-validation.md)。

## 冻结身份与实验范围

A100-SXM4-80GB、驱动 575.57.08、BF16、单卡；baseline
`T00-305bf8f9dc429f1af60e`，tolerance `T00-tol-fbd0eb931ac1c7153403`。
HF 使用官方 `official-flex-cu128-repaired-v1` 缓存和 FlexAttention，未替换成 dense。
HF/vLLM 在同一 GPU 上按子进程顺序执行，不同时占卡。

| 环境 | PyTorch | Transformers | Triton | tokenizers |
| --- | --- | --- | --- | --- |
| HF | 2.7.1+cu128 | 4.57.1 | 3.3.1 | 0.22.1 |
| vLLM（包元数据 0.26.0） | 2.11.0+cu130 | 5.14.1 | 3.6.0 | 0.22.2 |

引擎日志版本字符串为 `0.26.1.dev9+ga493692cf.d20260917`，与包元数据不同；
本轮源码身份以核验的 Git SHA 为准，不以版本字符串推断提交。
这些运行时差异是实际部署比较的一部分，不归因为 attention 单算子收益。
全部包版本保存在 `resume-20260918/performance-final/environment.json`；
HF/vLLM 包清单也保存在工作区 `*-freeze.txt`。测量时工作区只有验收文档改动，
`source-verification.json` 确认生产代码、实验脚本和测试均无 diff。
HF 完整依赖清单严格匹配 T00 lock，模型逐文件哈希、输入哈希和阈值未改。
5090 没有在本轮实测，A100 结果不代表 SM120。

## 已合入实现与本轮验收

以下修复已包含在上述代码提交中；2026-09-18 续跑没有修改生产代码。

1. 移除遗留的 8192 文本上限，使用 131072；单次 chunk 仍限 4096。
   standalone 页池按配置的最大上下文分配，短请求不会因全局上限扩大而默认分配 128K 页池。
2. V1 KV 提交原先按 36 层重复构造相同页组槽位，并在 GPU 上做动态布尔索引和页号检查。
   改为每次提交按共享页组复用映射、每种窗口复用保留位置；页号在 CPU 检查，
   新 KV 通过固定整数索引写入，空 summary 不执行无效写入。attention 数学计算未改。
3. 单请求 4K graph、128 输出、预热后五次的修复前诊断为
   49.61–50.04 ms/token，修复后为 15.02–15.10 ms/token；两次测量均包含 V1 调度。
   原始数据在 `smoke-graph-2/` 与 `smoke-graph-3/`。
   前者代表 T06 已交付的 V1 提交逻辑，非 T06 单模型计时；不能将两种计时范围混用。

4. BF16 prefill 的 query tile 从 32 扩大到 128，保留 64-key softmax 分块；
   32K TTFT 从 11.39–11.40 s 降至 6.608–6.615 s。128×128 双 stage 尝试因
   A100 共享内存不足而弃用，日志保留；生产采用 128×64、双 stage。
   混合批次跳过全部 query 都越界的 tile，避免短 decode 扫描无效的大段历史。
5. V1 图缓存最多保留 8 个形状，覆盖 8 请求逐个结束的 batch 大小；配置并发小于 4 时仍保留 4 个。
   调整前每次正式重复重捕获 8 次；调整后五次均为零。诊断计数通过快照保留，
   不再共享可变列表；对应 CPU 契约测试通过。

相对 T06：此前完整模型测量为 1K/4K、32 输出，本轮为五档长度、128 输出，
不直接混用计时范围计算发布版本加速比。16K–128K 的同范围 T06 端到端对照为 N/A；
上面的 4K 页提交诊断只用于解释验收期间的修复收益。

## 精度与功能

- 最终提交的 V1 分块 prefill 冻结教师位置比较 23/23 通过，包含 130944 输入和三组长距离检索输入；
  最大 logits 误差 6.6875、RMSE 2.03250、logprobs 误差 3.18721、top-1 分歧 margin 0.25。
  原始结果为 `accuracy-final/results.json`，记录干净工作区及最终提交身份。
- 最终扩展 KSA 测试 132/132 通过，包括真实 GPU、分页/短尾/summary 自身、
  非连续页、图重排与页复用；上限边界增加到 131072。
- 普通 Qwen3：确定性小权重、原生 `Qwen3ForCausalLM` / `GPUModelRunner`，两例 16-token
  生成的同前缀全词表 logprobs 对 HF 通过。最大误差 0.007792 / 0.004803，
  第一例 token 完全一致，第二例首处分歧是 margin 0.001953 的 BF16 近似并列。
  单独门限是 logprob 0.05 / margin 0.02；不是 KSA T00 容差修改。
  这是原生路径数值回归，不是预训练 Qwen3 能力评测。
  本轮复测记录在 `resume-20260918/plain-qwen3/results.json`，源码 SHA 与上述一致。

- 全长度真实缓存 decode：23 用例 × eager/graph，共 46/46 通过；
  最大 logits 误差 7.375、RMSE 2.16734、logprobs 误差 4.90624、top-1 分歧 margin 0.25。
  最终提交复测结果在 `decode-verified/results.json`。674219/674219 页归还，192 次 graph replay。

2026-09-18 继续运行中断的验收，保留旧 `graphs-retrieval/` 不完整记录；
新数据保存于 `resume-20260918/`，其 `run.sh` 和 `suite.log` 记录命令及进度。
完整 128-token eager/graph 比较 46/46 通过，38/46 组 token 完全一致；
其余同前缀分歧 margin 最大 0.25。最大 logits 误差 14.125、RMSE 2.54534、
logprobs 误差 5.90624，均在冻结阈值内。三组检索在三轮中 9/9 命中。
累计 graph replay 1480 次；精度矩阵切换请求长度和顺序发生 25 次捕获，
这不是性能计时，不能将其描述为稳态无重捕获。
HF 全轨迹复核 54/69 通过、15/69 失败：长度 1023/1024/1031/1032/1033
的 eager 和两轮 graph 均存在超限，最大 logits 误差 35.75，
长度 1032 的最大 logprobs 误差 14.35148。这 15 条失败轨迹实际选择 token 的 HF margin 均为 0，
但不能用 token 一致替代全词表数值门禁。全部 69 条实际轨迹逐步选择 token
的最大 HF margin 为 0.25；45/69 条与冻结 HF 生成 IDs 完全一致，
其余按实际相同前缀逐步比较，不混比已经分叉的生成轨迹。冻结阈值未修改。
结果在 `resume-20260918/generation-hf/results.json`，失败队列在 HF 阶段停止。

冻结上限：logits 最大绝对误差 20.33521、RMSE 3.70595、
logprobs 最大绝对误差 8.02013、top-1 分歧 margin 0.5。
以下取同一用例三个模式/重复中的最大值；五组均与冻结 HF 生成 IDs 完全一致。

| 输入长度 | 最大 logits 误差 | 最大 logprobs 误差 | 结果 |
| ---: | ---: | ---: | --- |
| 1023 | 35.750 | 5.328 | 失败 |
| 1024 | 28.750 | 5.000 | 失败 |
| 1031 | 28.375 | 4.938 | 失败 |
| 1032 | 33.625 | 14.351 | 失败 |
| 1033 | 27.750 | 4.813 | 失败 |

逐层诊断：长度 1032、生成步索引 114（文本位置 1145），第 17 层 MLP gate
单元 103 的输出 HF 为 -13.375、vLLM 为 37；经 SiLU 与 up 分支相乘后，
该层隐藏状态最大差从上一层约 1.06 放大到 3732.5。
这是单请求诊断中的误差放大位置，并非已确认的根因或有效修复。HF 同轨迹重跑 logits 误差为 0。
residual 的 BF16 舍入、HF RMSNorm、RoPE 初始化、拆分投影、SDPA/FP32 attention、完整 BF16 累加和 FP32 投影
对照都未使全部失败用例通过，不能把任一对照当成有效修复。诊断日志和逐层张量
保留在 `resume-20260918/diagnostic-*`、`trace-*`；生产源码未修改。

真实页压力、抢占重算、取消和复用通过，结束后 329 页全部归还。
graph/eager 基础 HTTP 服务均通过并发 1/4/8、SSE usage、固定 seed、
多样本与惩罚参数、echo/prompt logprobs、stop、断连取消与页回收。
HTTP 关闭时日志有 Python resource_tracker 的 semaphore 清理警告；
页回收断言通过，该警告未归类为 KV 泄漏。

## 性能与容量

单请求五档矩阵完成，16K/32K/64K graph decode 门禁全部通过。
4K–64K 另测 graph 并发 4/8；128K×4/8 按用户要求不执行，不计为通过。
每项一次预热、五次重复、128 输出、无提前 EOS。130944 + 128 = 131072。
HF 不支持的 batch 4/8 单列，不进行虚假的 HF 并发对照。

V1 时间包含 scheduler、采样、metadata、页提交及设备执行，不含 HTTP；
HF 时间包含官方 summary 插入、设备执行、argmax 与同步。
TTFT、稳态 TPOT、块边界 TPOT、总输出吞吐、峰值显存、在用/归还物理页、
加载/profiling 和图捕获均保留原始数据。最终性能门禁以 graph 生产配置判定，
eager 同时报告。

### 单请求性能

下表为五次正式重复的算术平均；单位 ms，decode 为 ms/token。

| 输入 token | HF TTFT | eager TTFT | graph TTFT | HF decode | eager decode | graph decode | graph 加速 |
| ---: | ---: | ---: | ---: | ---: | ---: | ---: | ---: |
| 4096 | 320.00 | 382.76 | 374.46 | 31.57 | 32.67 | 15.06 | 2.096× |
| 16384 | 1469.71 | 2302.83 | 2303.56 | 34.94 | 34.60 | 19.12 | 1.828× |
| 32768 | 3794.95 | 6612.56 | 6599.26 | 41.29 | 37.22 | 24.51 | 1.685× |
| 65536 | 11129.93 | 20996.73 | 20981.92 | 59.58 | 43.61 | 34.64 | 1.720× |
| 130944 | 36301.99 | 72665.29 | 72644.16 | 93.06 | 58.53 | 54.49 | 1.708× |

16K/32K/64K graph 最慢重复分别为 19.147/24.530/34.738 ms/token，
均快于 HF 最快重复的 34.840/41.200/59.544 ms/token。
所有单请求 graph 正式重复无新捕获，各模式结束时 KV 页全部归还。

4K eager decode 比 HF 慢约 3.5%；graph decode 五档均有收益。
graph TTFT 相对 HF 分别增加约 17%/57%/74%/89%/100%。
KSA 的 decode 图不覆盖 prefill；V1 分块调度、metadata/页提交及分页 attention
仍有额外开销，长窗口层继续计算全历史文本 attention。已有 query tile 优化
改善了 TTFT，但没有消除相对官方 FlexAttention prefill 的差距。
这些是不同固定运行时的端到端部署结果，不能全部归因为某一算子。

固定 128 输出的整请求吞吐如下；32K 及以上的 decode 收益未抵消 prefill 回退。

| 输入 token | HF 输出 token/s | graph 输出 token/s | graph / HF |
| ---: | ---: | ---: | ---: |
| 4096 | 29.568 | 55.968 | 1.893× |
| 16384 | 21.667 | 27.051 | 1.248× |
| 32768 | 14.162 | 13.180 | 0.931× |
| 65536 | 6.846 | 5.043 | 0.737× |
| 130944 | 2.660 | 1.609 | 0.605× |

### 并发性能与测量有效性

以下为 graph 的五次均值，TTFT 为请求平均首 token 延迟，decode 包含混合 prefill 与排队；
不能作为纯 decode kernel 延迟或与 HF batch=1 直接比较。吞吐按整批总输出除以耗时计算。

| 输入 token | 并发 | TTFT ms | decode ms/token | 输出 token/s | 峰值显存 GiB |
| ---: | ---: | ---: | ---: | ---: | ---: |
| 4096 | 4 | 1044.62 | 34.97 | 92.845 | 67.390 |
| 4096 | 8 | 1752.71 | 61.50 | 105.906 | 67.111 |
| 16384 | 4 | 6008.04 | 70.54 | 33.805 | 67.527 |
| 16384 | 8 | 11017.32 | 134.45 | 35.707 | 67.610 |
| 32768 | 4 | 17094.96 | 134.12 | 14.804 | 67.706 |
| 32768 | 8 | 31311.38 | 271.86 | 15.188 | 68.267 |
| 65536 | 4 | 53655.69 | 326.04 | 5.310 | 68.075 |
| 65536 | 8 | 99023.54 | 694.29 | 5.331 | 69.589 |

23/23 个范围内性能子进程正常退出，共 115 次正式测量；其中 HF 25 次、vLLM 90 次。
全部 vLLM 正式重复有效：无新图捕获、无 OOM、结束时页全部归还。128K×4/8 未测，
不把用户排除项计入分母。64K 并发 4→8 的吞吐收益很小，排队和 prefill 使延迟显著增加。

vLLM 加载与 profiling 为 10.724–14.793 s；
各图形状分配/预热为 87.64–222.51 ms，
捕获为 29.65–49.11 ms，均不计入正式重复。
Triton 首次 JIT 事件保留在预热日志中，未把每个编译事件另拆成独立耗时。
每次稳态/块边界 TPOT、峰值显存、最大在用页及归还数见逐次 CSV，启动细项见汇总 JSON。

### 容量

130944-token prompt 加 128 输出在 eager 五次正式重复中无 OOM，
graph 五次正式重复也无 OOM。prompt 后实际活跃 KV 为 7.214 GiB，
相同长度全层文本 KV 为 17.982 GiB，整体约 2.493× 压缩；
预分配 KV 池为 57.170 GiB，不能把预留池大小当作活跃 KV。
短窗口文本页受窗口和固定 prefill chunk 预算限制，summary 每 64 个文本 token
占一页（每页容纳 8 个 summary）；长窗口文本页保持线性增长。
首 token 探针包含最后一块 prefill 尚未在下一次调度释放的历史窗口页，
因此该测量包括额外 chunk 保留量，并非只按理论窗口计算。

## 原始证据与复现边界

版本库保留 [汇总指标](summary.json)、[逐次性能 CSV](performance.csv)、
[实际环境](environment.json) 和 [原始文件 SHA-256](raw-files.sha256)。
哈希路径相对工作区根目录，可在该目录运行 `sha256sum -c vllm/docs/ksa/results/T07/a100-final/raw-files.sha256` 核验。
`summary.json` 的总状态为 `needs_fix`；其内嵌
`performance_summary.decode_gate=pass` 只代表单请求 decode 性能门禁，不能覆盖精度失败。
原始张量、日志、命令脚本和环境锁留在工作区 `results/T07/`，不提交大型数据。

| 范围 | 工作区 `results/T07/` 下的原始路径 |
| --- | --- |
| 当前提交的分块 prefill / 真实缓存 decode | `accuracy-final/`、`decode-verified/` |
| eager/graph、检索和完整 HF 轨迹 | `resume-20260918/graphs-retrieval/`、`resume-20260918/generation-hf/` |
| 页压力、HTTP、普通 Qwen3 | `resume-20260918/pressure/`、`http-graph/`、`http-eager/`、`plain-qwen3/`（后三项同在续跑目录） |
| HF/vLLM 性能、加载与捕获、实际 KV | `resume-20260918/performance-final/` |
| 回归和契约测试 | `resume-20260918/unit.log`、`resume-20260918/contracts.log` |
| 失败定位及源码身份 | `resume-20260918/diagnostic-*.json`、`trace-*`、`source-verification.json`（同目录） |
| 实际续跑与范围调整 | `resume-20260918/run.sh`、`remaining.sh`、`finish.sh`、`scope-change.json`（同目录） |

单请求 HF 128K 子进程完成且 GPU 空闲后，控制器曾暂停以隔离数值诊断；
该任务 `processes.json.elapsed_s` 包含暂停等待，不应用作加载或推理耗时。
逐 token 计时未受影响，证据见 `orchestration-pause.json`。
128K 并发取消属于用户调整范围；控制器的终止记录不作为 OOM 或精度失败。
旧中断记录、失败诊断 traceback 均保留，不覆盖成成功记录。

## 最终门禁与后续修复范围

| 完成门禁 | 本轮结论 |
| --- | --- |
| 冻结精度、差异全部关闭 | 未通过：完整轨迹 15/69 超限，根因和有效修复未关闭 |
| 16K/32K/64K graph 单请求 decode 优于 HF 且超过波动 | 通过 |
| 实际 KV 压缩、128K 单请求无 OOM | 通过：约 2.493×，eager/graph 各五次 |
| 必需服务、生命周期、原生 Qwen3 与回归 | 通过；KSA 测试 132 项、契约测试 9 项 |
| TTFT、短上下文与 prefill 回退披露 | 已披露；32K 及以上整请求吞吐低于 HF |
| 128K×4/8 | 按用户要求不执行，不计为通过 |

完整 HF 生成轨迹仍有 15/69 超限，T07 保持 **NEEDS_FIX**。
已定位单请求中的误差放大位置，但尚未确认可通过冻结门禁的修复；
不能将这些差异视为已关闭，也不能调整 T00 阈值使其通过。
下一轮应围绕 1023/1024/1031/1032/1033 五组实际前缀逐层定位误差来源，
修复后重跑全 69 条 HF 轨迹、23 组 prefill、46 组缓存 decode 与 graph/服务回归。
若修改计时路径，需在新代码 SHA 和新结果目录复测性能门禁；本报告不替代该复测。
