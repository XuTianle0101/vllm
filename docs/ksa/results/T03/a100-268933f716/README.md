# T03 A100 分页缓存验收

状态：DONE。代码 SHA：`268933f7165264ff9bdd8b680fa77dfc70cdd647`。
本轮四个结果目录均记录同一 clean SHA、同一冻结基线与容差。

- 基线：`T00-305bf8f9dc429f1af60e`，容差：`T00-tol-fbd0eb931ac1c7153403`。
- A100-SXM4-80GB，BF16，TP=PP=1，eager 单请求，首次 prefill ≤4096，总文本 ≤8192。
- 63 项 CPU 测试及全部适用 pre-commit 检查通过。
- 四组各 17/17 教师强制通过。默认分页组 5/5 自由生成门禁通过，重复一致，
  所有生成 token 与 T02 一致；相同前缀的 T02 logits 最大绝对误差均为 0。
- HF 自由序列 exact equality 为 2/5；其余分叉与 T02 一致，冻结的相同前缀
  HF logits/logprobs/top-1 margin 门禁全部通过，没有更改容差。
- GPU 运行覆盖实际非连续物理页、淘汰前后、部分块和反复请求复用。
  每次教师强制请求释放后检查所有可分配页归还。
- CPU 测试补充 8K 存储容量、交错请求隔离、内存不足保留前缀、写失败释放、
  重复 clear 和 runner 异常清理。8K 存储测试不表示支持 8K 整段 prefill。

## 性能

128 token、关闭 EOS、一次预热、五次正式重复，GPU 实验顺序执行。
表内 TPOT 为稳态每次重复均值的均值 ± 样本标准差（ms），峰值为五次最大值。
HF/T02 为冻结验收归档；其余四组是本轮同 SHA 的独立开关实验。

| 路径 | 1K TPOT ms | 4K TPOT ms | 4K 峰值 GiB |
| --- | ---: | ---: | ---: |
| HF（归档） | 29.800 ± 0.154 | 29.720 ± 0.133 | 8.643 |
| T02 全历史（归档） | 41.042 ± 0.241 | 41.113 ± 0.040 | 16.040 |
| 本轮张量缓存 | 41.062 ± 0.117 | 41.343 ± 0.162 | 14.538 |
| 分页：逐层写入 | 47.925 ± 0.099 | 48.413 ± 0.085 | 14.726 |
| 分页：批量写入，保留 prefill 投影 | 44.911 ± 0.030 | 45.528 ± 0.212 | 16.571 |
| 分页：批量写入，压缩 prefill 待提交 KV（默认） | 44.060 ± 0.135 | 44.523 ± 0.026 | 14.726 |

批量写入相对逐层写入在 1K/4K 减少 3.864/3.890 ms，超过本次重复波动。
释放 prefill 投影视图相对保留它们，4K 峰值下降 1.845 GiB。
该 prefill 改动的 decode 时间差也单列在表中，但不据此宣称 decode 算法加速。
默认分页相对本轮张量缓存仍慢 2.999/3.180 ms；相对 T02 归档慢 3.019/3.409 ms。
因此不宣称端到端加速。默认分页相对 T02 的 4K 峰值减少 1.314 GiB。
TTFT、块边界 TPOT 和五次波动范围保存在 [结构化摘要](summary.json)。

## KV 占用

| 文本长度 | 短窗口每层文本页 | 每层 summary 页 | 长窗口每层文本页 | 全模型有效 KV | 全模型已分配页字节 | 预分配池 |
| --- | ---: | ---: | ---: | ---: | ---: | ---: |
| 1024 | 128 | 16 | 128 | 162 MiB | 162 MiB | 543.125 MiB |
| 4096 | 128 | 64 | 512 | 324 MiB | 324 MiB | 543.125 MiB |

文本页和 summary 页各存 8 行 KV；summary 页覆盖 64 个文本位置。
部分页的已分配字节可大于有效 KV，逐步数据在原始结果中。
短窗口文本页至多 129 页（包含当前部分块），summary 按 `ceil(N/64)` 页增长，
满足 `O(1024 + N/8)`。长窗口文本页随长度线性增长。
请求结束后有效 KV 与占用页归零，模型继续持有预分配总池。

## 原始证据与限制

结果根目录：`/workspace/volume/h20-data/xutianle/KSA/results/T03/`。
四个目录分别为 `a100-268933f716-paged`、`a100-268933f716-per-layer`、
`a100-268933f716-uncompacted`、`a100-268933f716-tensor`；对应日志为同名 `.log`。
摘要记录各原始 `results.json` 的 SHA256；分页结果还记录所用 T02 参考文件哈希。
T02 的 `timing.json` 混合 HF/vLLM 行，计算 T02 指标必须筛选 `backend == "vLLM"`。

已接入 vLLM `KVCacheManager`、cache spec、容量核算和请求释放，当前入口仍是
单请求 `KSAPythonRunner`，不是 `LLM.generate`/HTTP serving。
T04 负责批处理、chunked prefill、调度和抢占；T05/T06 再处理图与 attention 瓶颈。
当前 4096 prefill 上限、8192 总预算与平方级显式掩码阻断 16K/128K 模型实验，
未进行或宣称这些长度的性能测试。
