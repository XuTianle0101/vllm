# 实验结果

当前没有实际实验结果。

未来按 `Txx/<run-id>/` 保存每轮服务器反馈，结构见 [结果约定](../templates/result-contract.md)。每轮必须绑定实际提交 SHA 和 baseline ID，失败、OOM、中断结果也保留。

原始大体积 logits、trace 或模型数据默认保留在服务器，反馈中给出位置与必要摘要。此目录当前只有说明文件，不表示任何 ticket 已验证。
